apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  APP_COLOR: {{ .Values.config.APP_COLOR | quote }}
  {{ .Values.testfile.name }}: {{ .Values.testfile.content | quote }}
